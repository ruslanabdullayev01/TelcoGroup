@model PageNatedList<AboutUs>
@{
    ViewData["Title"] = "Index";
    int dataCount = ViewBag.DataCount;
    var headers = ViewBag.Headers;
}
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between">
                    <h2 class="card-title">@language.Getkey("AboutUs")</h2>
                    @if (dataCount == 0)
                    {
                        <a asp-area="TelcoAdmin" asp-controller="AboutUs" asp-action="Create" class="btn btn-primary text-white mr-2">@language.Getkey("Create")</a>
                    }
                </div>
                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>
                                    №
                                </th>
                                <th>
                                    @Html.Raw(language.Getkey("TopDescription"))
                                </th>
                                <th>
                                    @language.Getkey("Actions")
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (AboutUs aboutUs in Model)
                            {
                                <tr>
                                    <td>
                                        1
                                    </td>
                                    <td>
                                        @aboutUs.TopDescription
                                    </td>
                                    <td style="vertical-align: middle; text-align: center; ">
                                        <a asp-action="Detail" asp-route-id="@aboutUs.Id" class="btn btn-warning text-white mr-2">@language.Getkey("Details")</a>
                                        <a asp-action="Update" asp-route-id="@aboutUs.Id" class="btn btn-info text-white mr-2">@language.Getkey("Update")</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <div class="col-lg-12">
        <nav aria-label="Page navigation example">
            <ul class="pagination">
                @if (Model.HasPrev)
                {
                    <li class="page-item">
                        <a class="page-link" asp-area="TelcoAdmin" asp-controller="AboutUs" asp-action="Index"
                           asp-route-pageIndex="@(Model.PageIndex - 1)">@language.Getkey("Previous")</a>
                    </li>
                }
                @for (int i = Model.StartPage; i <= Model.EndPage; i++)
                {
                    <li class="page-item @(i == Model.PageIndex ? "active" : "")"><a class="page-link" asp-area="TelcoAdmin" asp-controller="AboutUs" asp-action="Index" asp-route-pageIndex="@i">@i</a></li>
                }
                @if (Model.HasNext)
                {
                    <li class="page-item">
                        <a class="page-link" asp-area="TelcoAdmin" asp-controller="AboutUs" asp-action="Index"
                           asp-route-pageIndex="@(Model.PageIndex + 1)">@language.Getkey("Next")</a>
                    </li>
                }
            </ul>
        </nav>
    </div>
    <h2 class="card-title">@language.Getkey("Headers")</h2>
    <div class="table-responsive pt-3">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>
                        @language.Getkey("BlueText")
                    </th>
                    <th>
                        @language.Getkey("GreenText")
                    </th>
                    <th>
                        @language.Getkey("Actions")
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (Header item in headers)
                {
                    <tr>
                        <td>
                            @Html.Raw(item.BlueText)
                        </td>
                        <td>
                            @Html.Raw(item.GreenText)
                        </td>
                        <td style="vertical-align: middle; text-align: center; ">
                            <a asp-action="Update" asp-controller="Headers" asp-route-id="@item.Id" class="btn btn-info text-white mr-2">@language.Getkey("Update")</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>