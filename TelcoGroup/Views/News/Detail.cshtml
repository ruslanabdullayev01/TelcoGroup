@model News
<section id="NewsDetail">
    <div class="allDetail">
        <h1>@Model.Title</h1>
        <div class="newsMainImage">
            <img src="~/src/images/news/@Model.ImagePath" alt="">
        </div>

        <p>@Html.Raw(Model.Description)</p>
    </div>
</section>